<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.herwinlab.covideveryday">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_covid"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_covid"
        android:supportsRtl="true"
        android:theme="@style/Theme.CovidEveryday">
        <activity
            android:name=".Dashboard"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".fragment.BedActivity"
            android:theme="@style/Theme.CovidEveryday"
            >

        </activity>

        <activity android:name=".ProfileApp"
            android:theme="@style/Theme.CovidEveryday"
            >

        </activity>

        <activity android:name=".fragment.DetailActivity"
            android:theme="@style/Theme.CovidEveryday">

        </activity>

    </application>

</manifest>